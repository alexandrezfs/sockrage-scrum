var ScrumServices = angular.module('ScrumServices', ['ngResource']);

/*
ScrumServices.factory('Example', ['$resource',

    function($resource){

        return $resource('/example/:id', {},
            {
                'query': { method: 'GET', isArray: true },
                'update': { method:'PUT' }
            });

    }]);
*/
